budget: 100.0
horizon_gw: 5
risk:
  profile: balanced
  ownership_weight: 0.0
news:
  weight: 0.5
  max_age_hours: 48
model:
  form_half_life_gw: 3
  form_max_weight: 0.6
  shrinkage_minutes: 900
  start_prior_games: 4.0
  horizon_decay: 0.85
optimizer:
  max_paid_hits: 2
  hit_cost: 4
  bench_weight: [0.20, 0.15, 0.05, 0.02]
  # R8 rolling transfer solver. Keep as a replay challenger until its terminal
  # free-transfer value has enough point-in-time evidence to promote live.
  multi_period_transfers: false
  multi_period_pool_size: 150
  multi_period_ft_value: 1.5
  # Minimum xP for a player allowed to sit on the bench. 0 disables it.
  # Raise to ~2.5 only while preparing a Wildcard or a Bench Boost: it buys a
  # strong bench with budget the starting XI would otherwise have, which is a
  # good trade the week you play the chip and a loss every other week.
  bench_floor_xp: 0.0
odds:
  provider: null
data:
  cache_ttl_hours: 6
  cache_ttl_matchday_hours: 1
entry_id: 1461088
free_transfers: 1
